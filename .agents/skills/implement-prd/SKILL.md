---
name: implement-prd
description: >-
  Implement one of this repo's own staged PRDs in `new-features/*.md` end to end. Read and
  verify the plan against the current code, work through its Stage sequence, resolve open
  questions empirically, add tests for each stage, keep the relevant suite green, update the
  PRD Progress log, and reconcile cross-referenced docs. Use for “implement new-features/X.md”,
  “work through this PRD”, or continuing a staged PRD. Do not use for an unverified external
  specification; use spec-to-staged-plan first.
---

# Implement a PRD

The canonical repository-specific instructions are maintained at
[`../../.claude/skills/implement-prd/SKILL.md`](../../../.claude/skills/implement-prd/SKILL.md).
Read that complete file before implementing a staged PRD. This portable entry point is kept in
`.agents` so agents that follow the Agent Skills convention discover the same workflow.
